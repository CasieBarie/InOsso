artifactId=${project.artifactId}
groupId=${project.groupId}
version=${project.version}

# -- Dependencies --
net.dv8tion_JDA.version=${version.jda}
dev.arbjerg_lavaplayer.version=${version.lavaplayer}
club.minnced_udpqueue-api.version=${version.udpqueue}
ch.qos.logback_logback-classic.version=${version.logback}
org.slf4j_slf4j-api.version=${version.slf4j}