name: Deploy

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      deploy: true
    secrets:
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}

  deploy:
    name: Deploy to Oracle
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy via SSH
        id: deploy
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ubuntu
          key: ${{ secrets.ORACLE_SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          envs: GHCR_TOKEN
          script_stop: false
          script: |
            DEPLOY_DIR="/home/ubuntu"
            GHCR_TOKEN_FILE="$DEPLOY_DIR/ghcr_token.txt"

            SSH_OK="❌ SSH Connection Failed"
            AUTH_OK="❌ Authentication Failed"
            TOKEN_OK="❌ Failed to Create GHCR Token"
            SCRIPT_OK="❌ Deployment Script Failed"
            CLEANUP_OK="❌ Cleanup Failed"

            if timeout 5 bash -c "</dev/tcp/127.0.0.1/22" 2>/dev/null; then
              SSH_OK="✅ SSH Connection Established"
            fi

            if whoami &>/dev/null; then
              AUTH_OK="✅ Authenticated with Private Key"
            fi

            if echo "$GHCR_TOKEN" > "$GHCR_TOKEN_FILE" && chmod 600 "$GHCR_TOKEN_FILE"; then
              TOKEN_OK="✅ GHCR Token Created"
            fi

            if [[ "$SSH_OK" == "✅"* && "$AUTH_OK" == "✅"* ]]; then
              if cd "$DEPLOY_DIR" && ./update-bot.sh; then
                SCRIPT_OK="✅ Deployment Script Executed (./update-bot.sh)"
              fi
            fi

            if [[ -f "$GHCR_TOKEN_FILE" ]]; then
              if shred -u "$GHCR_TOKEN_FILE"; then
                CLEANUP_OK="✅ Token File Cleaned Up"
              fi
            else
              CLEANUP_OK="✅ Nothing to Clean Up"
            fi

            if [[ "$SSH_OK" == "✅"* && "$AUTH_OK" == "✅"* && "$TOKEN_OK" == "✅"* && "$SCRIPT_OK" == "✅"* && "$CLEANUP_OK" == "✅"* ]]; then
              OVERALL_RESULT="✅ Deployment Completed Successfully"
              DEPLOY_EXIT_CODE=0
            else
              OVERALL_RESULT="❌ Deployment Failed"
              DEPLOY_EXIT_CODE=1
            fi

            {
              echo "SSH_OK=$SSH_OK"
              echo "AUTH_OK=$AUTH_OK"
              echo "TOKEN_OK=$TOKEN_OK"
              echo "SCRIPT_OK=$SCRIPT_OK"
              echo "CLEANUP_OK=$CLEANUP_OK"
              echo "OVERALL_RESULT=$OVERALL_RESULT"
              echo "DEPLOY_EXIT_CODE=$DEPLOY_EXIT_CODE"
            } >> $GITHUB_ENV

      - name: Deployment Summary
        if: always()
        run: |
          echo "# 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "### Deployment" >> $GITHUB_STEP_SUMMARY
          echo "- $SSH_OK" >> $GITHUB_STEP_SUMMARY
          echo "- $AUTH_OK" >> $GITHUB_STEP_SUMMARY
          echo "- $TOKEN_OK" >> $GITHUB_STEP_SUMMARY
          echo "- $SCRIPT_OK" >> $GITHUB_STEP_SUMMARY
          echo "- $CLEANUP_OK" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Result" >> $GITHUB_STEP_SUMMARY
          echo "- $OVERALL_RESULT" >> $GITHUB_STEP_SUMMARY
          if [[ "$DEPLOY_EXIT_CODE" -ne 0 ]]; then
            echo "Deployment failed — exiting with error"
            exit 1
          fi